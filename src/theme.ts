import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "Fira Code",
  },
  palette: {
    primary: {
      main: "#14213d",
    },
    secondary: {
      main: "#c6ff00",
    },
  },
});
